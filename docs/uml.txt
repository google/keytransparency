@startuml
[Web] -> [FE]
[Signer]

() "Reader"
() "Queuer"
() "Sequencer"
() "Appender"

database "Spanner" {
folder "Map" {
	[Mutations]
	[Queue]
	[Leafs]
	[Nodes]
	[SignedTreeHeads]
	}
folder "Privacy" {
	[Commitments]
}
}

FE --> Queuer
FE <-- Reader
FE <--> Committer
Signer --> Sequencer
Signer --> Appender

Appender <--> SignedTreeHeads

Queuer -> [Queue]
Queuer -> [Mutations]

Sequencer <- [Queue]
Sequencer -> [Leafs]
Sequencer -> [Nodes]

Reader <- [Leafs]
Reader <- [Nodes]
Reader <- Appender

Committer <- [Commitments]
Committer -> [Commitments]

@enduml
