@startuml
[Web] -> [FE]
[Signer]

() "Reader"
() "Queuer"
() "Sequencer"

database "Spanner" {
	[Mutations]
	[Queue]
	[Leafs]
	[Nodes]
	[Commitments]
}

FE --> Queuer
FE <-- Reader
Signer --> Sequencer

Queuer -> [Queue]
Queuer -> [Mutations]
Queuer -> [Commitments]

Sequencer <- [Queue]
Sequencer -> [Leafs]
Sequencer -> [Nodes]

Reader <- [Leafs]
Reader <- [Nodes]
Reader <- [Commitments]

@enduml
